version: 2

models:

  - name: stg_stronger_nudge
    description: 'staging table for salesforce stronger nudge'
    columns:
        - name: id
          description: 'object id'
          tests:
          - not_null
          - unique
  - name: stg_fund
    description: 'staging table for salesforce funds'
    columns:
        - name: id
          description: 'object id'
          tests:
          - not_null
          - unique
