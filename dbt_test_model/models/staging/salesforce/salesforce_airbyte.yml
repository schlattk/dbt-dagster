version: 2

models:

  - name: stg_airbyte_stronger_nudge
    description: 'staging table for salesforce stronger nudge'
    columns:
        - name: id
          description: 'object id'
          tests:
          - not_null
          - unique

  
